---
# packages to upgrade
update_distro_packages: "*"

# packages to exclude
update_distro_packages_excludes: []

# reboot if required
update_reboot_kernel: no

reboot_timeout: 600

centos_eol_vault_fix: yes  #temporary fix for CentOS systems before migration