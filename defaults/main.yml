---
# packages to upgrade
update_distro_packages: "*"

# packages to exclude
update_distro_packages_excludes: []

# reboot if required
update_reboot_kernel: false

reboot_timeout: 600

centos_eol_vault_fix: true  #temporary fix for CentOS systems before migration